TODO use cases with menus, toolbars, split panes, tables with viewports, tabs
TODO ability to add components to every component (as with TabbedPane, Toolbar)
TODO component(c).withSpec(spec), cell().with(c/"label"/...).withSpec(spec), container().with(menu/panel).add(...)
TODO what if empty line and then component - in which line will it be added
TODO how are those 'fancy borders' realized in airflite?